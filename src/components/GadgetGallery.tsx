import { useState } from 'react'
import { motion, useReducedMotion } from 'motion/react'
import { ArrowLeft, ArrowRight } from 'lucide-react'
const collections=[{name:'LA LUCE SI ATTACCA.',kind:'STICKER PACK',number:'01 / 03'},{name:'INDOSSA UN’IDEA.',kind:'TOTE BAG',number:'02 / 03'},{name:'FACCIAMOCI VEDERE.',kind:'CAMPAIGN POSTER',number:'03 / 03'}]
export function GadgetGallery(){const [selected,setSelected]=useState(0);const reduce=useReducedMotion();return <section id="irl" className="gadgets section-pad" aria-labelledby="gadgets-title"><div className="section-top"><span>FUORI DALLO SCHERMO</span><span>EDIZIONE 01</span></div><h2 id="gadgets-title">LUMINA <span className="yellow">IRL.</span></h2><p className="section-copy">Le idee escono dai corridoi.<br/>E vengono con te.</p><div className={`merch-stage merch-view-${selected}`}><motion.div className="merch-scene" key={selected} initial={reduce?false:{opacity:.3,scale:.97}} animate={{opacity:1,scale:1}} transition={{duration:.35}}>{selected===0?<><div className="merch-item sticker sticker-yellow"><span>FAI<br/>LUCE.</span><img src="/favicon.svg" alt=""/></div><div className="merch-item sticker sticker-black"><img src="/wordmark.svg" alt="Lumina"/><span>LISTA 01 — GENTILESCHI</span></div><div className="merch-item round-sticker"><span>01</span><small>LA TUA VOCE CONTA</small></div><div className="merch-item sticker sticker-small">OGNI IDEA<br/>MERITA LUCE.</div></>:selected===1?<div className="tote"><div className="tote-handle"/><div className="tote-body"><img src="/logo.svg" alt="Concept tote bag Lumina Lista 1"/><small>PORTA CON TE UN’IDEA.</small></div></div>:<div className="campaign-poster"><span>LICEO GENTILESCHI · NAPOLI</span><h3>OGNI<br/>IDEA<br/>MERITA<br/>LUCE.</h3><img src="/wordmark.svg" alt="Lumina"/><span>LISTA 01 / IL FUTURO SI ACCENDE</span></div>}</motion.div><span className="merch-concept">CONCEPT GRAFICO · NON IN VENDITA</span></div><div className="merch-controls"><div><span>{collections[selected].kind}</span><h3>{collections[selected].name}</h3></div><div className="merch-arrows"><button aria-label="Gadget precedente" onClick={()=>setSelected((selected+2)%3)}><ArrowLeft size={20}/></button><button aria-label="Gadget successivo" onClick={()=>setSelected((selected+1)%3)}><ArrowRight size={20}/></button></div></div><div className="merch-pagination" role="group" aria-label="Seleziona un gadget">{collections.map((item,i)=><button key={item.kind} aria-label={item.kind} aria-pressed={selected===i} className={selected===i?'is-active':''} onClick={()=>setSelected(i)}><span/></button>)}<span aria-live="polite">{collections[selected].number}</span></div></section>}
